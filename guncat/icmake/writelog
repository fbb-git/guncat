void writeLog()
{
    int idx;

    if (UNINSTALL == "")            // do not store uninstall info
        return;

    for (idx = sizeof(g_log); idx--; )
        fprintf(UNINSTALL, g_log[idx], "\n");
}




